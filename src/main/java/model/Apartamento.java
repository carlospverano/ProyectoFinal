package model;

public class Apartamento extends Vivienda{
    public Apartamento(String direccion, double valor, double area, Propietario propietario, String tipoPropiedad) {
        super(direccion, valor, area, propietario, tipoPropiedad);
    }
}
